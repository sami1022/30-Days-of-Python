# Day 2 - Input and Output 🐍

## 📌 Topics Covered
- Input Function
- Output Function
- User Input
- Type Casting

---

# 📖 Input in Python

The `input()` function is used to take input from the user.

Example:
```python
name = input("Enter your name: ")
```

---

# 📖 Output in Python

The `print()` function is used to display output.

Example:
```python
print("Hello")
```

---

# 📖 Type Casting

By default, input values are stored as strings.

To convert them:
```python
age = int(input("Enter age: "))
height = float(input("Enter height: "))
```

---

# 💻 Example Program

```python
name = input("Enter your name: ")
age = int(input("Enter your age: "))

print("Name:", name)
print("Age:", age)
```

---

# 📚 Key Points

- `input()` takes user input.
- `print()` displays output.
- Input values are strings by default.
- Use `int()` and `float()` for conversion.

---

# 🎯 Day 2 Completed

Practice more programs to improve input handling 🚀
